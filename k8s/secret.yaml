apiVersion: v1
kind: Secret
metadata:
  name: book-api-secret
  namespace: diploma-demo
type: Opaque
# Values are base64-encoded. Default: books / books
# To regenerate: echo -n "your-value" | base64
stringData:
  DB_USER: "books"
  DB_PASSWORD: "books"
  POSTGRES_DB: "booksdb"
