apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: diploma-demo
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
  clusterIP: None   # Headless service – required for StatefulSet DNS
